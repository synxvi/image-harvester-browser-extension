# 2026-05-21

- 将主开关从 chip-toggle 文字标签改为胶囊开关（capsule-toggle），纯 CSS 实现流畅切换动画
- 布局：左边 Off 右边 On，滑块通过 translateX(28px) 从左滑到右，0.3s cubic-bezier 缓动
- 清理了 JS 中 `enableToggleText` 和 `updateEnableText` 相关代码（胶囊文字通过 CSS 状态自动切换，无需 JS 动态改写）
- 不引入任何额外组件库
- 胶囊圆角改为 20px 与旁边 chip 按钮一致，滑块尺寸调为 28×22px（border-radius: 11px）适配全圆角风格
- 交换 top-row 左右位置：IMG/Video 按钮移到左侧，胶囊开关移到右侧
- 提交到 main 后创建 feature/flat-ui 分支做扁平化改造
- 扁平化改造：移除所有 border 描边，改用底色深浅区分层级
  - card hover 改为淡金色微高亮 box-shadow
  - 子保存目录输入框加白色背景区分，hover 加微高亮圈
- 配色改造：从暗沉暖棕/土色 → 明亮暖黄+黑+红点缀（取自图标配色）
  - accent: #B8860B → #E8A817（明亮黄）
  - bg: #F4F0E8 → #FFFDF5（明亮暖白）
  - surface-raised: #F9F6EF → #FFFAF0
  - surface-dim: #EDE8DE → #F5F0E6
  - text: #2C1810 → #1A1A1A（更纯的黑）
  - danger: #B83232 → #E53935（更鲜艳的红）
  - Header: 暗棕渐变 → 亮黄渐变(#F5C518→#E8A817→#D4940F)，文字从白改深色
  - 胶囊开关 Off 态: 灰棕滑块 → 红色滑块(#E53935)，底色从灰棕改浅粉(#F5E0DC)
  - action-exclusion: 暗棕 → 亮黄(#E8A817)
  - action-strategy: 暗绿 → 深灰(#4A4540)
  - 容器阴影从中性暖棕改为纯黑低透明度
- 悬浮下载按钮美化（content.css + content.js）
  - 单路径按钮: 黑底白边→亮黄底(#F5C518)+黑色SVG箭头图标（替换💾emoji），加毛玻璃效果
  - 多路径工具栏按钮: hover 从蓝紫(rgba(102,126,234))→亮黄(#E8A817)，加毛玻璃效果
  - Toast: 下载开始从蓝紫→亮黄，文字颜色适配深色
  - 整体配色与图标配色统一
- Bug修复：多路径开关被强制自动打开
  - 根因：updateMultiPathAvailability() 在 popup 初始化时被调用，如果用户之前手动关闭了多路径开关但已配置过路径，会自动把 ih_multi_path_enabled 强制写回 true
  - 修复：给函数加 isModeSwitch 参数，只有从模式切换（normal/canvas）调用时才自动恢复多路径，popup 初始化时不做
